publisher:
  select_full: |
    SELECT p.*, pp.*
      FROM `publisher` p
      LEFT JOIN `publisher` pp
        ON pp.`id` = p.`parent_id`

  select_full_by_pk: |
    @publisher/select_full
     WHERE p.`id` = ?

  select_full_by_name: |
    @publisher/select_full
     WHERE p.`name` = ?
